# tasks_2025_12_21_CSS改善計画

目的
- 全ページの見た目を統一し、現代的で読みやすいデザインに改善する。

前提
- 調査レポート（docs/reports/investigate/2025-12-21_CSSデザイン調査.md）に基づく
- 既存のテンプレートは `src/main/resources/templates` にある

タスク一覧（優先度順）
1. ベースCSS追加
   - ファイル: `src/main/resources/static/css/main.css` を作成
   - 内容: カラーパレット、タイポグラフィ（Google Fonts導入は最小限でローカルスタックを指定）、レスポンシブブレークポイント、ボタン・カード・フォームの基本スタイル
   - DoD: ブラウザで全テンプレートを開き、全体のフォント・色・ボタンが反映されること

2. テンプレートにCSSを読み込む
   - 変更ファイル: `src/main/resources/templates/fragments/header.html` または各テンプレに直接リンクを追加
   - DoD: すべてのテンプレートが `main.css` を読み込むこと

3. ヘッダー・フッターの共通スタイル適用
   - 変更ファイル: `templates/fragments/header.html` と `templates/fragments/footer.html`（存在しない場合は header のみ利用）
   - DoD: 各ページでヘッダーのレイアウトが統一され、レスポンシブ確認が通ること

4. 主要ページの調整（tictactoe, marubatsu, gomoku, index, login など）
   - 内容: カードレイアウト、ボタン整列、フォーム調整
   - DoD: ページごとに UI が崩れないことを確認する

5. 小さなアニメーションとトランジションの追加
   - 内容: ボタンの hover、カードの hover、フォーカスの視覚化
   - DoD: アニメーションがパフォーマンスに悪影響を与えないこと

6. アクセシビリティチェック
   - 内容: コントラスト、フォーカス、alt などを確認
   - DoD: 主要なページでコントラスト比が合格すること

関連ファイル
- `src/main/resources/static/css/i18n.css`（既存ファイル）
- `src/main/resources/templates/fragments/header.html`
- `src/main/resources/templates/*.html`

実施手順
1. main ブランチを確認後、新ブランチ `feat/css-improvements` を切る
2. `src/main/resources/static/css/main.css` を追加
3. `fragments/header.html` に CSS 読込を追加し、全テンプレートを確認
4. ページ単位で微調整

注意
- 実装範囲を超えないこと。今回は見た目改善に限定する。機能変更は行わない。

作成者: 自動生成プラン
作成日: 2025-12-21
