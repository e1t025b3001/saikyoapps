# 2025-12-21 CSSデザイン調査

目的
- 既存のテンプレート群（`src/main/resources/templates`）の見た目を改善するための設計指針と参考資料をまとめる。

調査概要
- 参考にした主なリソース
  - MDN CSS（日本語）: https://developer.mozilla.org/ja/docs/Web/CSS
  - CSS-Tricks: https://css-tricks.com/
  - Bootstrap（コンポーネント設計の参考）: https://getbootstrap.com/docs/5.0/getting-started/introduction/
  - Tailwind CSS（ユーティリティファーストの考え方）: https://tailwindcss.com/docs
  - Google Material Design（配色・間隔・動きの指針）: https://material.io/design
  - WebAIM（アクセシビリティ）: https://webaim.org/

発見したポイント（要約）
1. 基本レイアウト
   - Flexbox と CSS Grid を併用することで、レスポンシブな画面レイアウトがシンプルに実装できる。
   - ヘッダー、メイン、フッターは共通レイアウトとしてまとめるとテンプレート管理が楽になる。

2. タイポグラフィ
   - 余白・行間・フォントサイズの一貫性が視覚的な安定感を生む。
   - Google Fonts などで読みやすいフォントを選定し、CSS変数でフォントスタックを管理する。

3. 色と配色
   - カラーパレット（主色・アクセント・背景・テキスト）を定義して一元管理する（CSS変数推奨）。
   - コントラスト比（WCAG）を確認し、アクセシビリティを担保する。

4. コンポーネント化
   - ボタン、カード、フォーム入力などを共通コンポーネント化してスタイルを再利用する。
   - 小さなリッチ効果（ボックスシャドウ、境界線の丸み、微妙なグラデーション、ホバー/フォーカスのトランジション）で“デザイン感”を出す。

5. レスポンシブとモバイル優先設計
   - モバイルファーストでブレークポイントを設定する。
   - 画像とコンテナの幅の扱いを明確にする（max-width, width:100%など）。

6. 実装方針候補
   - 方針A: 独自CSSを1つのファイルにまとめ（例: `static/css/main.css`）、変数・ユーティリティを定義してテンプレートに導入する（軽量で自由度が高い）。
   - 方針B: Bootstrap をベースにカスタムテーマを当てる（既製のコンポーネントで素早く見栄えを整える）。
   - 方針C: Tailwind（ユーティリティ）を導入する（クラスで細かく制御できるが既存テンプレの改修が必要）。

7. アクセシビリティ
   - カラーコントラスト、キーボード操作、フォーカススタイルの確保。
   - 画像にaltを入れる、フォームにラベルを明示する等の基本を守る。

推奨事項（短期優先）
- まずは独自CSSファイル（`src/main/resources/static/css/main.css` または既存の `static/css/i18n.css` に併合）を作成して、全テンプレートへ共通で読み込む方式で統一する（方針A推奨）。
- 以下を最初の改善点として取り組む：
  1. ベースタイポグラフィ（フォント、行間、基本サイズ）を決める。
  2. ヘッダー／フッターの共通レイアウトを整える。
  3. ボタン／フォーム／カードの基本スタイルを作る。
  4. レスポンシブのブレークポイント（モバイル・タブレット・デスクトップ）を決める。

参考にするデザイン例
- CSS-Tricksの「A Complete Guide to Flexbox」や「A Complete Guide to Grid」
- Bootstrap コンポーネントページ（ボタン、フォーム、カード）
- Tailwind のコンポーネントギャラリーやPlayground

次のステップ（提案）
1. 計画フェーズ: `docs/personal_tasks/tasks_2025_12_21_CSS改善計画.md` を作り、実装タスクを小さく分割する。
2. 実装フェーズ: mainブランチから新しいブランチ（例: `feat/css-improvements`）を切って作業を行う。最初はベースCSSの追加とテンプレートへの適用を行う。

備考
- 本調査では具体的なコードは作成していない（調査フェーズのルールに準拠）。
- 実装にあたってユーザーの好み（色、フォント、テーマ例）があれば先に教えてください。

作成者: 自動生成レポート
作成日: 2025-12-21
